# Configure npm to handle platform-specific dependencies
node-linker=hoisted
legacy-peer-deps=true

# Don't ignore optional dependencies on Vercel
ignore-optional=false

# Force npm to use the exact versions specified
save-exact=true 